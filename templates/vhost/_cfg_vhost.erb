<% if @cfg_vhost -%><% cfg_vhost.sort.each do |key,value| -%>
<% if value.is_a?(Array) -%><% value.each do |v| -%>
  <%= "#{key} #{v}" -%>;
<% end -%><% else -%>
  <%= "#{key} #{value}" -%>;
<% end -%><% end -%><% end -%>
