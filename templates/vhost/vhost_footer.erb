}

<% if rewrite_www_to_non_www -%>
server {
  listen                <%= listen_ip %>;
  server_name           www.<%= vhost.gsub(/^www\./, '') %>;
  rewrite               ^ http://<%= vhost.gsub(/^www\./, '') %>$uri permanent;
}
<% end -%>
